# AlgoMind 开发文档

## 项目概述

**AlgoMind** 是一个面向算法学习的在线教育平台，采用 Vue 3 + Vite 技术栈构建。平台提供关卡挑战、视频课程、社区论坛、排行榜、错题本等功能，采用游戏化的学习方式帮助用户提升算法能力。

---

## 技术栈

| 类别 | 技术 |
|------|------|
| 框架 | Vue 3 (Composition API) |
| 构建工具 | Vite 6.2.0 |
| 状态管理 | Pinia 3.0.1 |
| 路由 | Vue Router 4.5.0 |
| HTTP 客户端 | Axios 1.8.4 |
| 代码规范 | ESLint 9.21.0 |

---

## 项目结构

```
algo-mind/
├── public/                     # 静态资源
│   └── favicon.ico
├── src/
│   ├── api/                    # API 接口层
│   │   └── index.js            # 统一 API 管理
│   ├── assets/                 # 样式资源
│   │   ├── base.css            # 基础样式
│   │   └── main.css            # 主样式文件
│   ├── components/             # 公共组件
│   │   ├── AIAssistant.vue     # AI 助手组件
│   │   └── CourseDetail.vue    # 课程详情组件
│   ├── router/                 # 路由配置
│   │   └── index.js            # 路由定义
│   ├── stores/                 # Pinia 状态管理
│   │   ├── user.js             # 用户状态
│   │   ├── level.js            # 关卡状态
│   │   ├── error.js            # 错题本状态
│   │   └── forum.js            # 论坛状态
│   ├── views/                  # 页面视图
│   │   ├── Login.vue           # 登录页
│   │   ├── Home.vue            # 首页
│   │   ├── Levels.vue          # 关卡页
│   │   ├── Challenge.vue       # 挑战页
│   │   ├── Courses.vue         # 课程页
│   │   ├── Forum.vue           # 论坛页
│   │   ├── Ranking.vue         # 排行榜页
│   │   ├── Errors.vue          # 错题本页
│   │   └── Profile.vue         # 个人中心
│   ├── App.vue                 # 根组件
│   ├── constants.js            # 全局常量
│   └── main.js                 # 应用入口
├── index.html                  # HTML 模板
├── package.json                # 依赖配置
├── vite.config.js              # Vite 配置
├── eslint.config.js            # ESLint 配置
└── jsconfig.json               # JS 配置
```

---

## 核心模块详解

### 1. 路由系统 (src/router/index.js)

路由采用 Vue Router 4 实现，支持路由守卫进行登录验证。

**路由表：**

| 路径 | 组件 | 说明 | 登录要求 |
|------|------|------|----------|
| /login | Login | 登录页面 | 否 |
| / | Home | 首页/仪表盘 | 是 |
| /levels | Levels | 关卡选择 | 是 |
| /challenge/:id | Challenge | 代码挑战 | 是 |
| /courses | Courses | 视频课程 | 是 |
| /forum | Forum | 学习社区 | 是 |
| /ranking | Ranking | 排行榜 | 是 |
| /errors | Errors | 错题本 | 是 |
| /profile | Profile | 个人中心 | 是 |

**路由守卫逻辑：**
- 检查 `localStorage` 中的 `isLoggedIn` 状态
- 未登录用户访问受保护路由时重定向到登录页
- 已登录用户访问登录页时重定向到首页

### 2. 状态管理 (Pinia Stores)

#### 2.1 用户状态 (src/stores/user.js)

管理用户登录状态、积分和用户信息。

**State：**
```javascript
{
  isLoggedIn: boolean,      // 登录状态
  points: number,           // 用户积分
  userInfo: Object | null   // 用户信息（名称、邮箱、等级等）
}
```

**核心方法：**
- `login(userData)` - 用户登录，持久化到 localStorage
- `logout()` - 用户登出，清除所有状态
- `setPoints(value)` - 设置积分
- `addPoints(value)` - 增加积分
- `deductPoints(value)` - 扣除积分
- `updateUserInfo(info)` - 更新用户信息

#### 2.2 关卡状态 (src/stores/level.js)

管理关卡进度、当前关卡和关卡数据。

**State：**
```javascript
{
  currentLevel: number,     // 当前关卡 ID
  completedLevels: number[], // 已完成关卡列表
  levelStatus: Object       // 各关卡状态（locked/unlocked/completed）
}
```

**核心方法：**
- `setCurrentLevel(id)` - 设置当前关卡
- `completeLevel(id)` - 标记关卡完成
- `unlockLevel(id)` - 解锁关卡
- `isLevelCompleted(id)` - 检查关卡是否完成
- `isLevelUnlocked(id)` - 检查关卡是否解锁
- `getLevelStatus(id)` - 获取关卡状态
- `resetProgress()` - 重置进度

#### 2.3 错题本状态 (src/stores/error.js)

管理用户错题记录。

**State：**
```javascript
{
  errorQuestions: Array     // 错题列表
}
```

**核心方法：**
- `addError(question)` - 添加错题
- `removeError(id)` - 移除错题
- `clearErrors()` - 清空错题本
- `hasError(id)` - 检查是否已存在
- `getErrorStats()` - 获取错题统计

#### 2.4 论坛状态 (src/stores/forum.js)

管理论坛帖子、评论和互动。

**State：**
```javascript
{
  posts: Array,             // 帖子列表
  currentPost: Object|null, // 当前查看的帖子
  categories: Array,        // 分类列表
  filters: Object           // 筛选条件
}
```

**核心方法：**
- `addPost(post)` - 发布帖子
- `deletePost(id)` - 删除帖子
- `addComment(postId, comment)` - 添加评论
- `likePost(id)` - 点赞帖子
- `setCategory(category)` - 设置分类筛选
- `setSort(sort)` - 设置排序方式
- `searchPosts(keyword)` - 搜索帖子

### 3. API 层 (src/api/index.js)

封装所有后端接口调用，基于 Axios 实现，支持请求拦截器和响应拦截器。

**API 配置：**
- **baseURL**: `/api`（通过 Vite 代理转发到后端）
- **timeout**: 10000ms
- **认证方式**: Bearer Token（从 localStorage 读取）

---

## 接口配置

### 一、用户认证接口

| 方法 | 路径 | 说明 | 请求参数 | 响应数据 |
|------|------|------|----------|----------|
| POST | `/login` | 用户登录 | `{username, password}` | `{token, user, points}` |

**登录示例：**
```javascript
// 请求
POST /login
Content-Type: application/json
{ "username": "admin", "password": "123456" }

// 响应
{
  "code": 0,
  "data": {
    "token": "uuid-token",
    "user": { "id": 1, "name": "系统管理员", "email": "admin@example.com", "points": 999 },
    "points": 999
  }
}
```

---

### 二、用户相关接口 (`/users/me/*`)

| 方法 | 路径 | 说明 | 请求参数 |
|------|------|------|----------|
| PUT | `/users/me` | 更新用户资料 | `{name, bio, email, gender, avatar, targetTrack, weeklyGoal, github, website}` |
| GET | `/users/me/heatmap?year=` | 获取做题热力图 | `year: 年份（可选）` |
| GET | `/users/me/stats` | 获取做题统计 | - |
| GET | `/users/me/activities?page=&pageSize=` | 获取活动列表 | `page, pageSize` |
| GET | `/users/me/status` | 获取用户状态 | - |
| PUT | `/users/me/status` | 更新用户状态 | `{emoji, mood, isBusy, busyAutoReply, busyEndTime}` |
| GET | `/users/me/ranking?track=` | 获取用户排名 | `track: 赛道筛选` |
| GET | `/users/me/points-history?page=&pageSize=` | 获取积分记录 | `page, pageSize` |
| GET | `/users/me/created-problems?page=&pageSize=` | 获取创建的题目 | `page, pageSize` |
| GET | `/users/me/solved-problems?page=&pageSize=` | 获取完成的题目 | `page, pageSize` |

**用户资料更新示例：**
```javascript
// 请求
PUT /users/me
{ 
  "name": "新名字", 
  "bio": "个人简介", 
  "email": "user@example.com",
  "targetTrack": "algo", 
  "weeklyGoal": 10,
  "github": "github.com/username",
  "website": "mywebsite.com"
}

// 响应
{ "code": 0, "data": { /* 更新后的用户对象 */ } }
```

**热力图数据格式：**
```javascript
// 响应
{
  "code": 0,
  "data": [
    { "date": "2024-01-01", "count": 5, "level": 2 },
    { "date": "2024-01-02", "count": 8, "level": 3 }
  ]
}
```

**用户统计数据格式：**
```javascript
// 响应
{
  "code": 0,
  "data": {
    "totalProblems": 156,
    "solvedProblems": 89,
    "createdProblems": 12,
    "totalPoints": 1250,
    "currentStreak": 7,
    "longestStreak": 23,
    "accuracy": 78.5,
    "weeklyGoal": 10,
    "weeklyProgress": 5,
    "trackStats": {
      "algo": { "solved": 45, "total": 60, "accuracy": 82.3 },
      "ds": { "solved": 28, "total": 50, "accuracy": 75.0 },
      "contest": { "solved": 16, "total": 46, "accuracy": 71.2 }
    }
  }
}
```

---

### 三、关卡相关接口

| 方法 | 路径 | 说明 | 请求参数 |
|------|------|------|----------|
| GET | `/levels?track=` | 获取关卡列表 | `track: algo/ds/contest（可选）` |
| POST | `/submit` | 提交答案判题 | `{levelId, answer, language?, stdinInput?}` |

**关卡列表响应：**
```javascript
{
  "code": 0,
  "data": [
    {
      "id": 1,
      "track": "algo",
      "order": 1,
      "name": "A1 时间复杂度基础",
      "isUnlocked": true,
      "rewardPoints": 18,
      "type": "single",
      "question": "以下哪一个是二分查找在有序数组中的时间复杂度？",
      "options": ["O(n)", "O(log n)", "O(n log n)", "O(1)"],
      "description": "理解查找算法复杂度"
    }
  ]
}
```

**提交答案示例：**
```javascript
// 请求
POST /submit
{
  "levelId": 1,
  "answer": "O(log n)",
  "meta": { "attempts": 1, "timeMs": 5000 }
}

// 响应（正确）
{
  "code": 0,
  "data": {
    "correct": true,
    "pointsEarned": 18,
    "nextLevelUnlocked": true,
    "starsEarned": 3,
    "bestStars": 3
  }
}
```

---

### 四、课程相关接口

| 方法 | 路径 | 说明 | 请求参数 |
|------|------|------|----------|
| GET | `/courses?track=&page=&pageSize=` | 获取课程列表 | `track, page, pageSize` |

**课程列表响应：**
```javascript
{
  "code": 0,
  "data": {
    "list": [
      {
        "id": 1,
        "title": "算法思维训练营（基础）",
        "description": "从零开始培养算法思维",
        "teacher": { "name": "陈老师", "title": "ACM金牌教练" },
        "track": "algo",
        "level": "beginner",
        "duration": "16课时",
        "rating": 4.8,
        "students": 1234
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 50
  }
}
```

---

### 五、论坛相关接口

| 方法 | 路径 | 说明 | 请求参数 |
|------|------|------|----------|
| GET | `/forum-posts?page=&pageSize=&sort=` | 获取帖子列表 | `page, pageSize, sort(latest/hot)` |
| POST | `/forum-posts` | 发布帖子 | `{author, authorLevel, avatar, topic, content, quote, tag}` |
| POST | `/forum-posts/{id}/like` | 点赞帖子 | `{action: "like"}` |
| POST | `/forum-posts/{id}/comment-count` | 评论计数+1 | - |

**帖子列表响应：**
```javascript
{
  "code": 0,
  "data": {
    "list": [
      {
        "id": 1,
        "author": "匿名牛油",
        "authorLevel": "LV.1",
        "avatar": "",
        "topic": "27届感觉看不到未来了",
        "content": "帖子内容...",
        "quote": "引用语",
        "tag": "# 实习分享",
        "likes": 520,
        "comments": 29,
        "liked": false,
        "createdAt": 1712345678000
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 100
  }
}
```

---

### 六、错题相关接口

| 方法 | 路径 | 说明 | 请求参数 |
|------|------|------|----------|
| GET | `/errors` | 获取错题列表 | - |
| GET | `/errors/{id}` | 获取单个错题 | - |
| POST | `/errors` | 添加错题 | `{levelId, question, userAnswer, description}` |
| PUT | `/errors/{id}` | 更新错题 | `{question, userAnswer, description, analysisStatus, analysis}` |
| DELETE | `/errors/{id}` | 删除错题 | - |
| POST | `/error-analysis` | AI分析错题 | `{errorId, description}` |

**错题对象结构：**
```javascript
{
  "id": 1,
  "levelId": 5,
  "question": "题目内容",
  "userAnswer": "用户的错误答案",
  "description": "错误描述",
  "createdAt": "2024-01-01T10:00:00",
  "analysisStatus": "已分析",
  "analysis": "AI分析结果..."
}
```

**AI分析响应：**
```javascript
{
  "code": 0,
  "data": {
    "errorId": 1,
    "analysis": "【AI 错题分析报告】\n\n一、错误原因分析...",
    "analyzedAt": "2024-01-01T10:30:00"
  }
}
```

---

### 七、学习计划接口

| 方法 | 路径 | 说明 | 请求参数 |
|------|------|------|----------|
| GET | `/learning-plans` | 获取用户所有学习计划 | - |
| GET | `/learning-plans/current?track=` | 获取当前赛道计划 | `track: algo/ds/contest` |
| POST | `/learning-plans/generate` | 生成新学习计划 | `{track, trackLabel, weeklyGoal}` |
| POST | `/learning-plans` | 保存学习计划 | `{track, trackLabel, weeklyGoal, weekGoals, dailyTasks, recommendations}` |
| DELETE | `/learning-plans/{id}` | 删除学习计划 | - |

**学习计划响应：**
```javascript
{
  "code": 0,
  "data": {
    "id": 1,
    "track": "algo",
    "trackLabel": "算法思维赛道",
    "weeklyGoal": 10,
    "weekGoals": [
      { "id": 1, "title": "掌握动态规划基础", "progress": 0, "target": 4 }
    ],
    "dailyTasks": [
      {
        "day": "今天",
        "tasks": [
          { "title": "动态规划基础概念", "duration": 30, "type": "learn" }
        ]
      }
    ],
    "recommendations": [
      { "type": "video", "title": "动态规划入门讲解", "source": "推荐" }
    ],
    "generatedAt": "2024-01-01T10:00:00"
  }
}
```

---

### 八、排行榜接口

| 方法 | 路径 | 说明 | 请求参数 |
|------|------|------|----------|
| GET | `/ranking?track=&period=` | 获取排行榜 | `track: all/algo/ds/contest, period: all/week/month` |

**排行榜响应：**
```javascript
{
  "code": 0,
  "data": [
    {
      "id": 1,
      "name": "系统管理员",
      "points": 999,
      "targetTrack": "algo",
      "rank": 1
    }
  ]
}
```

---

### 九、头像上传接口

| 方法 | 路径 | 说明 | 请求参数 |
|------|------|------|----------|
| POST | `/users/me/avatar` | 上传头像 | `file: MultipartFile` |
| GET | `/users/me/avatar` | 获取当前头像 | - |
| DELETE | `/users/me/avatar` | 删除头像 | - |

**上传限制：**
- 文件类型: `image/jpeg`, `image/png`, `image/gif`, `image/webp`
- 文件大小: 最大 5MB

**上传响应：**
```javascript
{
  "code": 0,
  "data": {
    "avatarUrl": "/uploads/avatar_1_a1b2c3d4.jpg",
    "message": "头像上传成功"
  }
}
```

**头像访问：**
上传后的头像可通过 `/uploads/{filename}` 路径访问，例如：
```
http://localhost:3000/api/uploads/avatar_1_a1b2c3d4.jpg
```

---

### 十、代码运行接口

| 方法 | 路径 | 说明 | 请求参数 |
|------|------|------|----------|
| POST | `/run-code` | 运行代码 | `{language, code}` |

**支持的语言：** `cpp`, `java`, `python`, `js`

**代码运行示例：**
```javascript
// 请求
POST /run-code
{
  "language": "python",
  "code": "print('Hello World')"
}

// 响应
{
  "code": 0,
  "data": {
    "output": "Hello World",
    "error": ""
  }
}
```

---

### 九、统一响应格式

所有接口统一返回格式：

```javascript
// 成功响应
{
  "code": 0,
  "data": { /* 业务数据 */ },
  "message": "success"
}

// 失败响应
{
  "code": 40001,
  "data": null,
  "message": "错误描述"
}
```

**错误码说明：**

| 错误码 | 说明 |
|--------|------|
| 0 | 成功 |
| 40001 | 参数错误 |
| 40002 | 编译失败 |
| 40401 | 资源不存在 |
| 50001 | 服务器内部错误 |

---

### 十、前端 API 调用示例

```javascript
import api from '@/api'

// 登录
const loginRes = await api.post('/login', { username: 'admin', password: '123456' })

// 获取热力图
const heatmapRes = await api.getUserProblemHeatmap(2024)

// 更新用户状态
await api.updateUserStatus({
  emoji: '😊',
  mood: 'happy',
  isBusy: false
})

// 提交答案
const submitRes = await api.post('/submit', {
  levelId: 1,
  answer: 'O(log n)'
})

// AI分析错题
const analysisRes = await api.analyzeError(1, '题目描述...')
```

### 4. 全局常量 (src/constants.js)

定义关卡数据结构，每个关卡包含：

```javascript
{
  id: number,               // 关卡 ID
  title: string,            // 标题
  description: string,      // 描述
  difficulty: string,       // 难度（入门/进阶/高级）
  tags: string[],           // 标签
  initialCode: string,      // 初始代码模板
  testCases: Object[],      // 测试用例
  hints: string[],          // 提示
  solution: string          // 参考答案
}
```

---

## 页面视图详解

### 1. 登录页 (Login.vue)

**功能：**
- 用户名/密码登录表单
- 表单验证
- 登录状态持久化
- 登录成功后跳转到首页

**样式特点：**
- 渐变背景
- 卡片式布局
- 输入框聚焦动画

### 2. 首页 (Home.vue)

**功能模块：**
- 欢迎区域（显示用户名和等级）
- 每日目标进度
- 推荐关卡卡片
- 学习统计（连续学习天数、完成题目数）
- 最近活动列表

**数据展示：**
- 使用进度条展示每日目标
- 使用卡片展示推荐关卡
- 使用列表展示最近活动

### 3. 关卡页 (Levels.vue)

**功能：**
- 网格布局展示所有关卡
- 关卡状态可视化（锁定/解锁/已完成）
- 难度筛选
- 点击关卡进入挑战

**关卡卡片信息：**
- 关卡编号
- 标题
- 难度标签
- 完成状态图标

### 4. 挑战页 (Challenge.vue)

**核心功能：**
- 代码编辑器区域
- 题目描述面板
- 测试用例展示
- 代码运行/提交
- AI 助手侧边栏

**交互逻辑：**
- 代码编辑实时保存
- 运行测试用例验证
- 通过所有测试后解锁下一关
- 失败时添加到错题本

### 5. 课程页 (Courses.vue)

**功能：**
- 课程分类筛选
- 课程卡片网格展示
- 搜索功能
- 课程详情弹窗

**课程信息：**
- 课程封面
- 标题和简介
- 讲师信息
- 评分和学员数
- 标签

### 6. 论坛页 (Forum.vue)

**功能：**
- 帖子列表展示
- 分类筛选（全部/提问/分享/讨论）
- 排序（最新/最热）
- 搜索功能
- 发布帖子

**帖子卡片：**
- 作者信息
- 标题和内容预览
- 标签
- 互动数据（点赞、评论）
- 发布时间

### 7. 排行榜页 (Ranking.vue)

**功能：**
- 用户排名列表
- 前三名特殊展示
- 当前用户排名高亮
- 积分和解题数展示

### 8. 错题本页 (Errors.vue)

**功能：**
- 错题列表展示
- 按时间/难度排序
- 重新练习按钮
- 移除错题

### 9. 个人中心 (Profile.vue)

**功能模块：**
- 用户信息编辑
- 学习统计（解题数、正确率、连续学习）
- 积分和等级展示
- 最近活动
- 技能雷达图

---

## 组件详解

### 1. AIAssistant.vue

AI 编程助手组件，提供智能提示和代码帮助。

**功能：**
- 消息对话界面
- 快速操作按钮（解释代码/优化代码/修复错误/提供思路）
- 消息历史记录
- 加载状态指示

**Props：**
- `code` - 当前代码内容

**Events：**
- 发送消息时根据类型提供不同帮助

### 2. CourseDetail.vue

课程详情组件，展示课程视频和相关信息。

**功能：**
- 视频播放器（含弹幕）
- 课程信息展示
- 课程目录
- 评论讨论区
- 学习笔记

**Props：**
- `course` - 课程数据对象

**Tabs：**
- 课程目录
- 讨论区
- 笔记

---

## 样式系统

### CSS 变量 (main.css)

```css
:root {
  --brand-blue: #4a6f9d;        /* 品牌主色 */
  --brand-blue-strong: #3f5d85; /* 品牌深色 */
  --brand-accent: #6672cb;      /* 强调色 */
  --brand-warm: #d88966;        /* 暖色调 */
  
  --text-title: #1f2f44;        /* 标题文字 */
  --text-main: #2f425b;         /* 正文文字 */
  --text-sub: #5f7286;          /* 次要文字 */
  --text-muted: #667789;        /* 弱化文字 */
  
  --line: #d4dde8;              /* 边框线 */
  --line-soft: #e1e8f1;         /* 浅色边框 */
  --line-card: #dde6f2;         /* 卡片边框 */
  
  --panel-bg: #f3f6fa;          /* 面板背景 */
  --card-bg: #ffffff;           /* 卡片背景 */
  --app-bg: linear-gradient(180deg, #f8fafc 0%, #eef2f6 100%);
}
```

### 通用类

- `.page-container` - 页面容器（最大宽度 1200px）
- `.surface-card` - 卡片样式（圆角、阴影）
- `.section-title` - 区块标题

---

## 开发规范

### 代码风格

- 使用 Composition API (`<script setup>`)
- 组件名使用 PascalCase
- 文件名使用 PascalCase（组件）或 camelCase（JS）
- 使用 2 空格缩进
- 使用单引号

### 状态管理规范

- 所有状态变更通过 Store 方法
- 持久化状态使用 `localStorage`
- 状态变更后触发 UI 更新

### 组件开发规范

- Props 使用 `defineProps` 定义类型
- Emits 使用 `defineEmits` 定义
- 使用 `ref` 和 `computed` 管理响应式数据
- 生命周期钩子使用 `onMounted`、`onUnmounted`

---

## 构建与部署

### 开发命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 代码检查
npm run lint
```

### 构建配置

- **Vite 配置**：支持 `@` 别名指向 `src` 目录
- **ESLint**：使用 `@eslint/js` 和 `eslint-plugin-vue`

---

## 功能特性

### 已实现功能

1. **用户系统**
   - 登录/登出
   - 用户信息管理
   - 积分系统

2. **关卡系统**
   - 渐进式关卡解锁
   - 代码挑战
   - 测试用例验证
   - AI 助手辅助

3. **课程系统**
   - 视频课程浏览
   - 课程搜索筛选
   - 课程详情查看
   - 弹幕评论

4. **社区系统**
   - 论坛发帖
   - 评论互动
   - 点赞功能
   - 分类筛选

5. **学习辅助**
   - 错题本记录
   - 排行榜激励
   - 学习统计
   - 个人中心

### 待扩展功能

- [ ] 真实后端 API 对接
- [ ] 代码编辑器增强（语法高亮、自动补全）
- [ ] 视频播放器功能完善
- [ ] 实时弹幕系统
- [ ] 用户头像上传
- [ ] 消息通知系统

---

## 数据流说明

```
用户操作 → 组件事件 → Store 方法 → State 更新 → UI 响应
                ↓
            API 调用（模拟/真实）
                ↓
            localStorage 持久化
```

---

## 注意事项

1. **登录状态**：刷新页面后登录状态通过 localStorage 恢复
2. **数据持久化**：用户进度、错题本等数据存储在 localStorage
3. **响应式设计**：页面适配移动端（最大宽度限制）
4. **模拟数据**：当前使用模拟数据，后续需对接真实 API

---

## 作者

**AlgoMind Team**

---

*文档版本：v2.0*  
*最后更新：2026-04-02*
